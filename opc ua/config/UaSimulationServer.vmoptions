# Enter one VM parameter per line
# For example, to adjust the maximum memory usage to 512 MB, uncomment the following line:
# -Xmx512m
# To include another file, uncomment the following line:
# -include-options [path to other .vmoption file]
#
# Workaround for Java 11 and JavaFX 11+
#  http://mail.openjdk.java.net/pipermail/openjfx-dev/2018-October/022713.html 
# (also happens in other than Eclipse)
-Djava.library.path=C:/workaround_javafx11_non_existing_folder
